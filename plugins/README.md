# plugins

自研插件规范源。计划包含：

- `dsh-image-bridge/`：图片附件桥 + generate_image 工具
- `dsh-memory-panel/`：纯本地文件记忆面板
- `dsh-skill-router/`：技能路由提示段
- `dsh-image-vision/`：整合识图插件（已有独立 GitHub 仓，可 submodule）
- `dsh-routing-suite/`：注入器 + 路由预设套装

> 当前这些插件源码仍主要在 `F:\tools\<name>`。
> 本目录是迁移目标；迁移完成前以 `F:\tools` 内为准。

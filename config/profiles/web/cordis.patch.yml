# Your patch layer for this dsh profile, applied after every bundle layer:
# a top-level YAML array of loader patch entries (id-targeted config
# overrides, disables, and insert lists; `!!js` expressions allowed).
# 技能路由提示段（普通 Cordis 插件，非 bundle，经 patch 装配）。
- insert:
    - id: dsh-skill-router
      name: '@dsh-external/dsh-skill-router'
# DeepSeek Harness EAC companion plugins (synced from Deepseek-Harness-EAC assets/plugins)
# web 壳桥：纯 web 环境补全 window.dshDesktop（余额推送 / 一键还原 / 系统打开），
# 需先于 balance / client-file-changes 注册，使客户端组件挂载时桥已就位。
- insert:
    - id: web-shell-bridge
      name: '@deepseek-ai/dsh-web-shell-bridge'
- insert:
    - id: balance
      name: '@deepseek-ai/dsh-balance'
- insert:
    - id: file-changes
      name: '@deepseek-ai/dsh-file-changes'
- insert:
    - id: client-file-changes
      name: '@deepseek-ai/dsh-client-file-changes'
- insert:
    - id: shell-terminal
      name: '@deepseek-ai/dsh-shell-terminal'
- insert:
    - id: easy-setup
      name: '@deepseek-ai/dsh-easy-setup'
- insert:
    - id: task-notify
      name: '@deepseek-ai/dsh-task-notify'
# 2026-08-18 本地维护：已删除 dsh-web-ui-all 内不再使用的子插件（pet 宠物 /
# describe-image 内置识图 / ui-dsh-aionui-panel 右侧面板）——对应装配行已从
# @linxin666/dsh-web-ui-all/cordis.patch.yml 移除，node_modules 包已删。
# 2026-08-21 dsh-web-ui-all 升级 0.2.7：聚合 patch 的 id 改为 web-ui-* 前缀，
# 继续禁用宠物 / 内置识图 / AionUI 右侧面板 / 自带 better-sidebar（保留本地 0.12.2）。
- id: web-ui-pet
  disabled: true
- id: web-ui-describe-image
  disabled: true
- id: web-ui-dsh-aionui-panel
  disabled: true
- id: web-ui-better-sidebar
  disabled: true

# docs/agents

Agent 协作约定（issue tracker、triage labels、domain docs）。

当前阶段直接复用 `F:\tools\docs\agents\` 下的约定：

- `issue-tracker.md`
- `triage-labels.md`
- `domain.md`

待 dsh-local 独立后，可把这些文档复制/迁移到本目录并改为指向 `xsoc1/tools-tickets`。
